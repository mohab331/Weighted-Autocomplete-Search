class AppConstants {
  static const String appTitle = 'Github User Search';
  static const Duration searchDelayTimer = Duration(milliseconds: 700);

  /// Assuming that item is frequent when it appears 3 times
  static const int frequentSearchCount = 3;
}
